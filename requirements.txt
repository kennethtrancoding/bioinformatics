# Direct dependencies only. pandas, biopython, pyyaml, click and tqdm were
# listed here but imported nowhere -- snakemake pulls in the ones it needs
# itself, and re-declaring them made this file look like a record of what the
# code uses when it was not one.
snakemake>=7.32.4
numpy>=1.24          # workflow/helpers/run_estimate_net.py
requests>=2.31
flask>=3.0
flask-limiter>=3.5
gunicorn>=21.0
openpyxl>=3.1        # reads the sequencing company's checksum workbook
boto3>=1.34
